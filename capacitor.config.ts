import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.starbookmark.app',
  appName: '별의 갈피',
  webDir: 'out',
  android: {
    path: 'android',
  },
};

export default config;
